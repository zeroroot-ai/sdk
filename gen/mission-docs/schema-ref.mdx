---
title: Schema Reference
description: Every field of every mission-related message.
---

# Mission Schema Reference

Generated from the canonical proto at `gibson/mission/v1/mission_definition.proto` and `gibson/daemon/v1/daemon.proto`.

## `gibson.mission.v1.AgentNodeConfig`

AgentNodeConfig contains configuration for agent nodes. AGENT = LLM-driven worker that calls tools and plugins on the author's behalf. The executor selects an agent component by name and dispatches the configured Task.

| Field | Type | Tag | Description |
|---|---|---|---|
| `agent_name` | `string` | 1 | AgentName is the name of the agent to execute |
| `task` | `gibson.types.v1.Task` | 2 | Task is the agent task configuration |
| `max_tokens_per_call` | `int32` | 3 | max_tokens_per_call is the per-node override of MissionConstraints.max_tokens_per_call for this agent node only.  When present (non-nil), this value is used as the effective cap for all LLM calls made by this node, regardless of the mission-level MissionConstraints.max_tokens_per_call value. Setting this to 0 explicitly disables the cap for this node (the mission-level cap is NOT applied as a fallback when this field is explicitly set to 0).  When absent (nil / proto3 optional not set), the mission-level MissionConstraints.max_tokens_per_call applies instead.  0 = inherit from mission-level (when this field is absent). Spec: mission-schema-canonicalization Requirement 5; gibson#133. |
| `llm_slots` | `repeated gibson.mission.v1.LLMSlotConfig` | 5 | llm_slots pins LLM provider/model bindings per named slot for this agent node. Each entry maps a slot name to a specific provider+model. Multiple entries allow different slots to use different providers/models on the same node. Empty list → the node inherits the tenant's default provider for all slots. Absent slot entries fall through to tenant default + constraint search over the tenant's permitted providers.  Empty provider or model within an entry is a valid fall-through marker: the daemon resolves it against the tenant's configured defaults.  Precedence at resolution per slot: explicit binding in llm_slots > tenant default > constraint search over the tenant's permitted providers.  Spec: sdk#260 (multi-slot LLM binding contract); consumer: gibson#524. |

## `gibson.mission.v1.ConditionNodeConfig`

ConditionNodeConfig contains configuration for condition nodes

| Field | Type | Tag | Description |
|---|---|---|---|
| `expression` | `string` | 1 | Expression to evaluate (e.g., "result.status == 'success'") |
| `true_branch` | `repeated string` | 2 | TrueBranch contains node IDs to execute if condition is true |
| `false_branch` | `repeated string` | 3 | FalseBranch contains node IDs to execute if condition is false |
| `language` | `gibson.mission.v1.Language` | 4 | Language declares the expression language. Defaults to LANGUAGE_CEL; LANGUAGE_UNSPECIFIED is treated as CEL for backwards compatibility with pre-Language-field documents. |

## `gibson.mission.v1.DataPolicy`

DataPolicy defines how data is handled for a node.  Deprecated: superseded by the ECS brain (gibson#851, ADR-0008). Data handling (reuse + scoping) is now implicit in the event-sourced World and scope-relative identity (ADR-0002) + ambient projection. Retained wire-compatibly; ignored by the engine.

| Field | Type | Tag | Description |
|---|---|---|---|
| `store_input` | `bool` | 1 | StoreInput determines whether to store input data in GraphRAG |
| `store_output` | `bool` | 2 | StoreOutput determines whether to store output data in GraphRAG |
| `retention` | `google.protobuf.Duration` | 3 | Retention specifies how long to retain data (0 = forever) |
| `encryption` | `bool` | 4 | Encryption determines whether data should be encrypted at rest |
| `access_control` | `repeated string` | 5 | AccessControl specifies who can access this data |

## `gibson.mission.v1.JobNodeConfig`

RetryPolicy defines the retry behavior for a mission node JobNodeConfig contains configuration for job nodes. JOB = a unit of work driven on a bank of always-on coding agents (gibson#1706). The executor opens a job on the bank, then runs the verify loop: it dispatches the acceptance step to the declared verifier component, and on failure sends the verifier's report as the next input to the SAME job, so the agent keeps its conversation and its worktrees. When the verifier passes, or the passes run out, the executor calls CloseJob with a verdict and a score.  The loop lives inside this node. The mission graph stays a DAG: a job node never adds a loop edge.

| Field | Type | Tag | Description |
|---|---|---|---|
| `bank_ref` | `string` | 1 | bank_ref names the bank to open the job on, by id or by name. Required. |
| `spec` | `gibson.job.v1.JobSpec` | 2 | spec is the structured input that opens the job: goal, repositories, credential names, World inputs, and the acceptance the verify loop runs against. Required. |
| `constraints` | `gibson.types.v1.TaskConstraints` | 3 | constraints bound every turn of the job. The daemon clamps them to the bank policy. Unset means the bank default. |

## `gibson.mission.v1.JoinNodeConfig`

JoinNodeConfig blocks until every node ID in `wait_for` has completed (success or final failure), then merges their results per `strategy`. JOIN is a first-class noun separable from PARALLEL — a JOIN can merge results from non-parallel branches.  Spec: mission-verb-noun-registry Requirement 7.

| Field | Type | Tag | Description |
|---|---|---|---|
| `wait_for` | `repeated string` | 1 | wait_for lists the upstream node IDs whose completion is required before this JOIN runs. Must be non-empty; submit-time validation rejects empty wait_for. |
| `strategy` | `gibson.mission.v1.MergeStrategy` | 2 | strategy selects how upstream results are combined. |
| `aggregator` | `string` | 3 | aggregator carries a CEL expression used when strategy is MERGE_STRATEGY_CUSTOM. The expression sees `sources` (a map from node ID to that node's result) and returns the merged value. Empty when strategy is not CUSTOM. |

## `gibson.mission.v1.LLMSlotConfig`

LLMSlotConfig pins the provider/model for an agent node's LLM slot. It is a tenant-scoped reference: the provider name must be one the tenant has configured (gibson.tenant provider config); model is the provider's model id (empty = the provider's default model). Leave the whole message unset to inherit the tenant default.

| Field | Type | Tag | Description |
|---|---|---|---|
| `slot` | `string` | 1 | slot is the slot name this binding targets. Empty defaults to "primary". |
| `provider` | `string` | 2 | provider is the configured provider's name for the calling tenant. |
| `model` | `string` | 3 | model is the model id; empty uses the provider's default model. |

## `gibson.mission.v1.MissionConstraints`

MissionConstraints declares the operational limits baked into a mission definition. Making constraints part of the schema lets mission authors publish self-describing missions (e.g., "stop after 50 findings") without requiring callers to supply limits out-of-band at dispatch time.  Semantics (zero means unlimited): - max_duration  : 0 duration  → no time limit - max_tokens    : 0           → no token budget - max_cost      : 0.0         → no cost ceiling - max_findings  : 0           → no finding count limit  Spec: sdk#47 (MissionConstraints proto promotion).

| Field | Type | Tag | Description |
|---|---|---|---|
| `max_duration` | `google.protobuf.Duration` | 1 | max_duration is the wall-clock limit for the entire mission. Uses google.protobuf.Duration for sub-second precision. Absent or zero-value means no time limit. |
| `max_tokens` | `int64` | 2 | max_tokens is the cumulative LLM token budget across the entire mission (all agent nodes combined). The daemon accumulates usage on every LLM invocation and stops the mission when the budget is exceeded. 0 means unlimited. This is a mission-wide budget, not a per-call limit; use max_tokens_per_call to cap individual invocations. |
| `max_cost` | `double` | 3 | max_cost is the cumulative LLM cost ceiling in USD across all agent nodes. 0.0 means unlimited. |
| `max_findings` | `int32` | 4 | max_findings is the maximum number of findings to collect before the mission stops. 0 means unlimited. |
| `severity_threshold` | `string` | 5 | severity_threshold is the minimum severity level required to record a finding. Common values: "low", "medium", "high", "critical". Empty string means accept all severities. |
| `require_evidence` | `bool` | 6 | require_evidence indicates whether all findings must include proof-of-concept evidence before being recorded. |
| `blocked_tools` | `repeated string` | 7 | blocked_tools lists tool names that must not be invoked during this mission. The daemon enforces this at dispatch time. |
| `blocked_domains` | `repeated string` | 8 | blocked_domains lists network domains (e.g., "prod.example.com") that agents must not contact. Enforcement is best-effort at the tool level. |
| `max_turns_per_agent` | `int32` | 9 | max_turns_per_agent caps the number of agent turns (Observe→Think→Act iterations) for any single agent node in the mission. 0 means unlimited. |
| `allowed_techniques` | `repeated string` | 10 | allowed_techniques is the allowlist of attack technique IDs (taxonomy) that agents may use during the mission. Empty list means no allowlist (any technique may be used unless blocked). |
| `blocked_techniques` | `repeated string` | 11 | blocked_techniques is the blocklist of attack technique IDs that agents must not use, regardless of allowed_techniques. Empty list means no blocklist. |
| `max_tokens_per_call` | `int32` | 12 | max_tokens_per_call is the per-invocation cap on LLM tokens for any single LLM call within this mission. Applied by the daemon before every provider call; the provider never sees more than this many output tokens.  Precedence cascade (highest → lowest): 1. Per-node *NodeConfig.max_tokens_per_call (when set on a specific node) 2. This field (mission-level default) 3. 0 — no cap from this mechanism  When a per-node override is set it completely supersedes this field for that node (including 0, which explicitly disables the cap for that node while this field may still apply to all other nodes).  This field is different from max_tokens: max_tokens is a cumulative budget for the entire mission; max_tokens_per_call is a ceiling on each individual LLM call.  0 means unlimited at this level. Spec: mission-schema-canonicalization Requirement 5. Enforced by EffectivePerCallCap() (wired in M4, gibson#133). |

## `gibson.mission.v1.MissionDefinition`

MissionDefinition represents a mission template/definition. This is the shareable mission specification that can be created via the CreateMissionDefinition API and referenced by mission runs.

| Field | Type | Tag | Description |
|---|---|---|---|
| `id` | `string` | 1 | ID is the unique identifier for this mission definition |
| `name` | `string` | 2 | Name is a human-readable name for the mission |
| `description` | `string` | 3 | Description provides additional context about what this mission does |
| `version` | `string` | 4 | Version is the semantic version of the mission definition |
| `target_ref` | `string` | 5 | TargetRef is a reference to the target (name or ID) This needs to be resolved to a TargetID when creating a mission instance |
| `nodes` | `map<string, gibson.mission.v1.MissionNode>` | 6 | Nodes contains all the nodes in the mission, indexed by node ID |
| `edges` | `repeated gibson.mission.v1.MissionEdge` | 7 | Edges contains all the directed edges connecting nodes in the mission |
| `entry_points` | `repeated string` | 8 | EntryPoints contains the IDs of nodes that can serve as entry points to the mission These are nodes with no incoming edges |
| `exit_points` | `repeated string` | 9 | ExitPoints contains the IDs of nodes that can serve as exit points from the mission These are nodes with no outgoing edges |
| `metadata` | `map<string, string>` | 10 | Metadata contains additional custom metadata for the mission |
| `dependencies` | `gibson.mission.v1.MissionDependencies` | 11 | Dependencies specifies required agents and tools for this mission |
| `source` | `string` | 12 | Source is the git URL this mission was installed from (if applicable) |
| `installed_at` | `google.protobuf.Timestamp` | 13 | InstalledAt is the timestamp when this mission was installed |
| `created_at` | `google.protobuf.Timestamp` | 14 | CreatedAt is the timestamp when the mission definition was created |
| `workspace` | `gibson.mission.v1.WorkspaceConfig` | 15 | Workspace configures repository cloning + workspace management for agents that need code access. Optional; missions without code interaction omit this field.  Spec: mission-schema-canonicalization (mirror migration). |
| `constraints` | `gibson.mission.v1.MissionConstraints` | 16 | Constraints declares mission-level operational limits. When present, these are the authoritative constraints for the mission DAG — they make the mission self-describing so authors do not have to supply limits out-of-band at dispatch time. The daemon merges these with any dispatch-time overrides (dispatch wins on conflict). Optional: absent means no constraints are baked into the definition.  Spec: sdk#47 (MissionConstraints proto promotion). |
| `decider_slot` | `gibson.mission.v1.LLMSlotConfig` | 17 | DeciderSlot names the mission-level LLM the brain's Decider runs on — the orchestration decision-maker, distinct from per-node agent slots (gibson#850). Only the provider+model are used (the slot name is implicitly "decider"). Absent means the brain uses the tenant's dashboard-default provider/model, so missions need not set it. |

## `gibson.mission.v1.MissionDependencies`

MissionDependencies specifies required components for a mission

| Field | Type | Tag | Description |
|---|---|---|---|
| `agents` | `repeated string` | 1 | Agents lists required agent components by name or URL |
| `tools` | `repeated string` | 2 | Tools lists required tool components by name or URL |
| `plugins` | `repeated string` | 3 | Plugins lists required plugin components by name or URL |

## `gibson.mission.v1.MissionEdge`

MissionEdge represents a directed edge in the mission DAG.  The graph is acyclic. A loop is never an edge. Work that repeats until it passes an acceptance step loops INSIDE one node: a job node runs its verify loop against the same job (gibson#1706), and a node's RetryPolicy retries the node itself.

| Field | Type | Tag | Description |
|---|---|---|---|
| `from` | `string` | 1 | From is the source node ID |
| `to` | `string` | 2 | To is the destination node ID |
| `condition` | `string` | 3 | Condition is an optional condition that must be satisfied for the edge to be traversed |
| `metadata` | `map<string, string>` | 4 | Metadata contains additional metadata for the edge |

## `gibson.mission.v1.MissionNode`

MissionNode represents a single node in a mission DAG

| Field | Type | Tag | Description |
|---|---|---|---|
| `id` | `string` | 1 | ID is the unique identifier for this node within the mission |
| `type` | `gibson.mission.v1.NodeType` | 2 | Type is the node type |
| `name` | `string` | 3 | Name is a human-readable name for the node |
| `description` | `string` | 4 | Description provides additional context about this node |
| `agent_config` | `gibson.mission.v1.AgentNodeConfig` | 5 | AgentConfig for agent nodes |
| `tool_config` | `gibson.mission.v1.ToolNodeConfig` | 6 | ToolConfig for tool nodes |
| `plugin_config` | `gibson.mission.v1.PluginNodeConfig` | 7 | PluginConfig for plugin nodes |
| `condition_config` | `gibson.mission.v1.ConditionNodeConfig` | 8 | ConditionConfig for condition nodes |
| `parallel_config` | `gibson.mission.v1.ParallelNodeConfig` | 9 | ParallelConfig for parallel nodes |
| `join_config` | `gibson.mission.v1.JoinNodeConfig` | 15 | JoinConfig for join nodes (mission-verb-noun-registry). Field number 15 because 10-14 are sibling MissionNode fields (dependencies, timeout, retry_policy, data_policy, metadata). |
| `job_config` | `gibson.mission.v1.JobNodeConfig` | 17 | JobConfig for job nodes (gibson#1706). Field number 17 because 16 is the sibling reuse_policy field. |
| `dependencies` | `repeated string` | 10 | Dependencies lists node IDs that must complete before this node executes |
| `timeout` | `google.protobuf.Duration` | 11 | Timeout is the maximum execution time for this node |
| `retry_policy` | `gibson.mission.v1.RetryPolicy` | 12 | RetryPolicy defines retry behavior for this node |
| `data_policy` | `gibson.mission.v1.DataPolicy` | 13 | DataPolicy defines data handling policy for this node.  Deprecated: data reuse + scoping are no longer node-declared. Under the ECS brain (gibson#851, ADR-0008) reuse is implicit in the event-sourced World and scoping flows from scope-relative identity (ADR-0002) + ambient projection. The field is retained wire-compatibly for old definitions but is ignored by the engine. |
| `metadata` | `map<string, string>` | 14 | Metadata contains additional custom metadata for this node |
| `reuse_policy` | `gibson.mission.v1.ReusePolicy` | 16 | ReusePolicy declares how this node's I/O is scoped + reused across mission runs.  Deprecated: superseded by the ECS brain (gibson#851, ADR-0008). Reuse is implicit in the World; scoping is via scope-relative identity (ADR-0002) + ambient projection. Retained wire-compatibly but ignored by the engine. |

## `gibson.mission.v1.ParallelNodeConfig`

ParallelNodeConfig contains configuration for parallel nodes. PARALLEL fans out to its sub-nodes concurrently, capped by max_concurrency. Sibling failures are isolated (one failing sub-node does not cancel its siblings). Spec: mission-verb-noun-registry Requirement 6.

| Field | Type | Tag | Description |
|---|---|---|---|
| `sub_nodes` | `repeated gibson.mission.v1.MissionNode` | 1 | SubNodes contains the nodes to execute in parallel |
| `max_concurrency` | `int32` | 2 | MaxConcurrency limits the number of concurrent executions (0 = unlimited) |

## `gibson.mission.v1.PluginNodeConfig`

PluginNodeConfig contains configuration for plugin nodes. PLUGIN = multi-method provider keyed by `plugin_name + method`. Distinct from TOOL: a plugin advertises several callable methods behind one component identity. The executor selects the named method and dispatches `params` as the call payload.

| Field | Type | Tag | Description |
|---|---|---|---|
| `plugin_name` | `string` | 1 | PluginName is the name of the plugin to query |
| `method` | `string` | 2 | Method is the plugin method to call |
| `params` | `map<string, string>` | 3 | Params contains the method parameters |
| `max_tokens_per_call` | `int32` | 4 | max_tokens_per_call is the per-node override of MissionConstraints.max_tokens_per_call for this plugin node only. Follows the same semantics as AgentNodeConfig.max_tokens_per_call: present and non-zero caps the call; present and 0 disables the cap for this node; absent means fall through to the mission-level constraint. Spec: mission-schema-canonicalization Requirement 5; gibson#133. |

## `gibson.mission.v1.RepositoryConfig`

RepositoryConfig defines a single repository to clone. Maps directly to the SDK's workspace.RepositoryConfig type.

| Field | Type | Tag | Description |
|---|---|---|---|
| `name` | `string` | 1 | Name is the unique identifier for this repository within the mission. Required. |
| `url` | `string` | 2 | URL is the Git repository URL (HTTPS or SSH). Required. |
| `branch` | `string` | 3 | Branch is the Git branch to checkout after cloning. Defaults to the repository's default branch when empty. |
| `credential_name` | `string` | 4 | CredentialName references a credential in the credential store. Optional — public repos don't need credentials. |
| `shallow` | `bool` | 5 | Shallow enables `git clone --depth 1`. |
| `depends_on` | `repeated string` | 6 | DependsOn lists repository names that must clone first. Enables topological ordering for multi-repo missions. |

## `gibson.mission.v1.RetryPolicy`

| Field | Type | Tag | Description |
|---|---|---|---|
| `max_retries` | `int32` | 1 | MaxRetries is the maximum number of retry attempts |
| `backoff_strategy` | `gibson.mission.v1.BackoffStrategy` | 2 | BackoffStrategy determines how delays are calculated between retries |
| `initial_delay` | `google.protobuf.Duration` | 3 | InitialDelay is the delay before the first retry attempt |
| `max_delay` | `google.protobuf.Duration` | 4 | MaxDelay is the maximum delay between retry attempts (used for exponential backoff) |
| `multiplier` | `double` | 5 | Multiplier is the factor by which the delay increases (used for exponential backoff) |

## `gibson.mission.v1.ReusePolicy`

ReusePolicy declares how a node's I/O is scoped + reused across mission runs.  Deprecated: superseded by the ECS brain (gibson#851, ADR-0008). Reuse is implicit in the event-sourced World and scoping flows from scope-relative identity (ADR-0002) + ambient projection, so node-declared reuse/scoping no longer has meaning. Retained wire-compatibly; ignored by the engine.

| Field | Type | Tag | Description |
|---|---|---|---|
| `output_scope` | `string` | 1 | OutputScope: "mission_run" | "mission" | "global". Default: "mission". |
| `input_scope` | `string` | 2 | InputScope: "mission_run" | "mission" | "global". Default: "mission". |
| `reuse` | `string` | 3 | Reuse: "skip" | "rerun" | "merge". Controls behavior when an existing output is found in scope. Default: "rerun". |

## `gibson.mission.v1.ToolNodeConfig`

ToolNodeConfig contains configuration for tool nodes. TOOL = single-purpose named function with a typed input map. The executor invokes the named tool through the tool worker queue and returns the tool's output as the node result.

| Field | Type | Tag | Description |
|---|---|---|---|
| `tool_name` | `string` | 1 | ToolName is the name of the tool to execute |
| `input` | `map<string, string>` | 2 | Input contains the tool input parameters |
| `max_tokens_per_call` | `int32` | 3 | max_tokens_per_call is the per-node override of MissionConstraints.max_tokens_per_call for this tool node only. Follows the same semantics as AgentNodeConfig.max_tokens_per_call: present and non-zero caps the call; present and 0 disables the cap for this node; absent means fall through to the mission-level constraint. Spec: mission-schema-canonicalization Requirement 5; gibson#133. |

## `gibson.mission.v1.WorkspaceConfig`

WorkspaceConfig configures repository cloning + workspace management for missions whose agents need to interact with code. The daemon's workspace manager honors this at mission start (initializeWorkspaces).  Spec: mission-schema-canonicalization (mirror migration — lifts mission.WorkspaceConfig from the daemon's hand-written mirror into the canonical proto schema).

| Field | Type | Tag | Description |
|---|---|---|---|
| `repositories` | `repeated gibson.mission.v1.RepositoryConfig` | 1 | Repositories to clone for this mission. Each entry becomes a workspace addressable from agents via harness.Workspace(name). |
| `settings` | `gibson.mission.v1.WorkspaceSettings` | 2 | Settings carries workspace-wide knobs (cleanup, LSP, isolation). |

## `gibson.mission.v1.WorkspaceSettings`

WorkspaceSettings carries workspace-wide options.

| Field | Type | Tag | Description |
|---|---|---|---|
| `cleanup_on_complete` | `bool` | 1 | CleanupOnComplete deletes workspace directories after the mission ends. Defaults to true at the daemon when unset (cleanup is the safe default). |
| `use_worktrees` | `bool` | 2 | UseWorktrees enables Git worktrees for per-agent isolation — concurrent modifications without conflicts. |
| `lsp_enabled` | `bool` | 3 | LSPEnabled starts language servers for code validation. |
| `lsp_timeout` | `google.protobuf.Duration` | 4 | LSPTimeout caps LSP validation duration. Encoded as a protobuf Duration to preserve sub-second precision. |
| `base_directory` | `string` | 5 | BaseDirectory is the workspace clone root. When empty the daemon uses a temp directory. |

