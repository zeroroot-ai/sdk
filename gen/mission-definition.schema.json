{
  "$defs": {
    "Acceptance": {
      "additionalProperties": false,
      "description": "Acceptance is how a job is judged. The job node executor runs the verify loop against it. A person or an agent that closes a job by hand reads it as guidance.",
      "properties": {
        "maxPasses": {
          "description": "max_passes is how many verify passes the job node executor runs before it closes the job as FAILED. Zero means one pass. This bounds the verify loop INSIDE one job. A mission node's RetryPolicy is a different thing: it retries the whole node.",
          "format": "int32",
          "minimum": 0,
          "type": "integer"
        },
        "passingScore": {
          "description": "passing_score is the score, from 0.0 to 1.0, at or above which the verifier accepts the work.",
          "format": "double",
          "type": "number"
        },
        "verifierComponent": {
          "description": "verifier_component names the component that judges the work, in the slash form \"agent/\u003cname\u003e\". Empty means no verify loop: a person or an agent closes the job.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "AgentNodeConfig": {
      "additionalProperties": false,
      "description": "AgentNodeConfig contains configuration for agent nodes. AGENT = LLM-driven worker that calls tools and plugins on the author's behalf. The executor selects an agent component by name and dispatches the configured Task.",
      "properties": {
        "agentName": {
          "description": "AgentName is the name of the agent to execute",
          "type": "string"
        },
        "llmSlots": {
          "description": "llm_slots pins LLM provider/model bindings per named slot for this agent node. Each entry maps a slot name to a specific provider+model. Multiple entries allow different slots to use different providers/models on the same node. Empty list → the node inherits the tenant's default provider for all slots. Absent slot entries fall through to tenant default + constraint search over the tenant's permitted providers.  Empty provider or model within an entry is a valid fall-through marker: the daemon resolves it against the tenant's configured defaults.  Precedence at resolution per slot: explicit binding in llm_slots \u003e tenant default \u003e constraint search over the tenant's permitted providers.  Spec: sdk#260 (multi-slot LLM binding contract); consumer: gibson#524.",
          "items": {
            "$ref": "#/$defs/LLMSlotConfig"
          },
          "type": "array"
        },
        "maxTokensPerCall": {
          "description": "max_tokens_per_call is the per-node override of MissionConstraints.max_tokens_per_call for this agent node only.  When present (non-nil), this value is used as the effective cap for all LLM calls made by this node, regardless of the mission-level MissionConstraints.max_tokens_per_call value. Setting this to 0 explicitly disables the cap for this node (the mission-level cap is NOT applied as a fallback when this field is explicitly set to 0).  When absent (nil / proto3 optional not set), the mission-level MissionConstraints.max_tokens_per_call applies instead.  0 = inherit from mission-level (when this field is absent). Spec: mission-schema-canonicalization Requirement 5; gibson#133.",
          "format": "int32",
          "minimum": 0,
          "type": "integer"
        },
        "task": {
          "$ref": "#/$defs/Task",
          "description": "Task is the agent task configuration"
        }
      },
      "type": "object"
    },
    "ConditionNodeConfig": {
      "additionalProperties": false,
      "description": "ConditionNodeConfig contains configuration for condition nodes",
      "properties": {
        "expression": {
          "description": "Expression to evaluate (e.g., \"result.status == 'success'\")",
          "type": "string"
        },
        "falseBranch": {
          "description": "FalseBranch contains node IDs to execute if condition is false",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "language": {
          "description": "Language declares the expression language. Defaults to LANGUAGE_CEL; LANGUAGE_UNSPECIFIED is treated as CEL for backwards compatibility with pre-Language-field documents.",
          "enum": [
            "LANGUAGE_CEL"
          ],
          "type": "string"
        },
        "trueBranch": {
          "description": "TrueBranch contains node IDs to execute if condition is true",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "DataPolicy": {
      "additionalProperties": false,
      "description": "DataPolicy defines how data is handled for a node.  Deprecated: superseded by the ECS brain (gibson#851, ADR-0008). Data handling (reuse + scoping) is now implicit in the event-sourced World and scope-relative identity (ADR-0002) + ambient projection. Retained wire-compatibly; ignored by the engine.",
      "properties": {
        "accessControl": {
          "description": "AccessControl specifies who can access this data",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "encryption": {
          "description": "Encryption determines whether data should be encrypted at rest",
          "type": "boolean"
        },
        "retention": {
          "description": "Retention specifies how long to retain data (0 = forever)",
          "type": "string"
        },
        "storeInput": {
          "description": "StoreInput determines whether to store input data in GraphRAG",
          "type": "boolean"
        },
        "storeOutput": {
          "description": "StoreOutput determines whether to store output data in GraphRAG",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "JobNodeConfig": {
      "additionalProperties": false,
      "description": "RetryPolicy defines the retry behavior for a mission node JobNodeConfig contains configuration for job nodes. JOB = a unit of work driven on a bank of always-on coding agents (gibson#1706). The executor opens a job on the bank, then runs the verify loop: it dispatches the acceptance step to the declared verifier component, and on failure sends the verifier's report as the next input to the SAME job, so the agent keeps its conversation and its worktrees. When the verifier passes, or the passes run out, the executor calls CloseJob with a verdict and a score.  The loop lives inside this node. The mission graph stays a DAG: a job node never adds a loop edge.",
      "properties": {
        "bankRef": {
          "description": "bank_ref names the bank to open the job on, by id or by name. Required.",
          "minLength": 1,
          "type": "string"
        },
        "constraints": {
          "$ref": "#/$defs/TaskConstraints",
          "description": "constraints bound every turn of the job. The daemon clamps them to the bank policy. Unset means the bank default."
        },
        "spec": {
          "$ref": "#/$defs/JobSpec",
          "description": "spec is the structured input that opens the job: goal, repositories, credential names, World inputs, and the acceptance the verify loop runs against. Required."
        }
      },
      "required": [
        "spec"
      ],
      "type": "object"
    },
    "JobSpec": {
      "additionalProperties": false,
      "description": "JobSpec is the structured input that opens a job: the goal, the repositories and credentials the work needs, the World nodes it starts from, and how it is judged. The daemon reads repositories and credential_names before it mints the per-turn grant, so they are declared fields. context is free-form. Anything the daemon must enforce lives in a declared field.  A JobSpec says WHAT to do. It carries no execution bounds. Bounds are gibson.types.v1.TaskConstraints and they ride on the dispatch envelope (gibson.types.v1.Task.constraints), which the daemon fills from the bank policy and from the mission node. This package imports nothing above gibson.common.v1 on purpose, so gibson.types.v1.Task can carry a JobSpec without a cyclic import.",
      "properties": {
        "acceptance": {
          "$ref": "#/$defs/Acceptance",
          "description": "acceptance is how the job is judged."
        },
        "context": {
          "additionalProperties": {
            "$ref": "#/$defs/TypedValue"
          },
          "description": "context is free-form context for the member. The daemon does not read it.",
          "type": "object"
        },
        "credentialNames": {
          "description": "credential_names lists the credentials the member may read during the job. The per-turn grant covers these and no others.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "goal": {
          "description": "goal is what the job must achieve, in plain words.",
          "type": "string"
        },
        "inputs": {
          "description": "inputs lists World node ids the job starts from: findings, plans, earlier deliverables.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "repositories": {
          "description": "repositories lists the repositories the job works in.",
          "items": {
            "$ref": "#/$defs/RepositorySpec"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "JoinNodeConfig": {
      "additionalProperties": false,
      "description": "JoinNodeConfig blocks until every node ID in `wait_for` has completed (success or final failure), then merges their results per `strategy`. JOIN is a first-class noun separable from PARALLEL — a JOIN can merge results from non-parallel branches.  Spec: mission-verb-noun-registry Requirement 7.",
      "properties": {
        "aggregator": {
          "description": "aggregator carries a CEL expression used when strategy is MERGE_STRATEGY_CUSTOM. The expression sees `sources` (a map from node ID to that node's result) and returns the merged value. Empty when strategy is not CUSTOM.",
          "type": "string"
        },
        "strategy": {
          "description": "strategy selects how upstream results are combined.",
          "enum": [
            "MERGE_STRATEGY_CONCAT",
            "MERGE_STRATEGY_REDUCE",
            "MERGE_STRATEGY_FIRST",
            "MERGE_STRATEGY_LAST",
            "MERGE_STRATEGY_CUSTOM"
          ],
          "type": "string"
        },
        "waitFor": {
          "description": "wait_for lists the upstream node IDs whose completion is required before this JOIN runs. Must be non-empty; submit-time validation rejects empty wait_for.",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "type": "array"
        }
      },
      "type": "object"
    },
    "LLMSlotConfig": {
      "additionalProperties": false,
      "description": "LLMSlotConfig pins the provider/model for an agent node's LLM slot. It is a tenant-scoped reference: the provider name must be one the tenant has configured (gibson.tenant provider config); model is the provider's model id (empty = the provider's default model). Leave the whole message unset to inherit the tenant default.",
      "properties": {
        "model": {
          "description": "model is the model id; empty uses the provider's default model.",
          "type": "string"
        },
        "provider": {
          "description": "provider is the configured provider's name for the calling tenant.",
          "type": "string"
        },
        "slot": {
          "description": "slot is the slot name this binding targets. Empty defaults to \"primary\".",
          "type": "string"
        }
      },
      "type": "object"
    },
    "MissionConstraints": {
      "additionalProperties": false,
      "description": "MissionConstraints declares the operational limits baked into a mission definition. Making constraints part of the schema lets mission authors publish self-describing missions (e.g., \"stop after 50 findings\") without requiring callers to supply limits out-of-band at dispatch time.  Semantics (zero means unlimited): - max_duration  : 0 duration  → no time limit - max_tokens    : 0           → no token budget - max_cost      : 0.0         → no cost ceiling - max_findings  : 0           → no finding count limit  Spec: sdk#47 (MissionConstraints proto promotion).",
      "properties": {
        "allowedTechniques": {
          "description": "allowed_techniques is the allowlist of attack technique IDs (taxonomy) that agents may use during the mission. Empty list means no allowlist (any technique may be used unless blocked).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "blockedDomains": {
          "description": "blocked_domains lists network domains (e.g., \"prod.example.com\") that agents must not contact. Enforcement is best-effort at the tool level.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "blockedTechniques": {
          "description": "blocked_techniques is the blocklist of attack technique IDs that agents must not use, regardless of allowed_techniques. Empty list means no blocklist.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "blockedTools": {
          "description": "blocked_tools lists tool names that must not be invoked during this mission. The daemon enforces this at dispatch time.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxCost": {
          "description": "max_cost is the cumulative LLM cost ceiling in USD across all agent nodes. 0.0 means unlimited.",
          "format": "double",
          "type": "number"
        },
        "maxDuration": {
          "description": "max_duration is the wall-clock limit for the entire mission. Uses google.protobuf.Duration for sub-second precision. Absent or zero-value means no time limit.",
          "type": "string"
        },
        "maxFindings": {
          "description": "max_findings is the maximum number of findings to collect before the mission stops. 0 means unlimited.",
          "format": "int32",
          "minimum": 0,
          "type": "integer"
        },
        "maxTokens": {
          "description": "max_tokens is the cumulative LLM token budget across the entire mission (all agent nodes combined). The daemon accumulates usage on every LLM invocation and stops the mission when the budget is exceeded. 0 means unlimited. This is a mission-wide budget, not a per-call limit; use max_tokens_per_call to cap individual invocations.",
          "format": "int64",
          "minimum": 0,
          "type": "integer"
        },
        "maxTokensPerCall": {
          "description": "max_tokens_per_call is the per-invocation cap on LLM tokens for any single LLM call within this mission. Applied by the daemon before every provider call; the provider never sees more than this many output tokens.  Precedence cascade (highest → lowest): 1. Per-node *NodeConfig.max_tokens_per_call (when set on a specific node) 2. This field (mission-level default) 3. 0 — no cap from this mechanism  When a per-node override is set it completely supersedes this field for that node (including 0, which explicitly disables the cap for that node while this field may still apply to all other nodes).  This field is different from max_tokens: max_tokens is a cumulative budget for the entire mission; max_tokens_per_call is a ceiling on each individual LLM call.  0 means unlimited at this level. Spec: mission-schema-canonicalization Requirement 5. Enforced by EffectivePerCallCap() (wired in M4, gibson#133).",
          "format": "int32",
          "minimum": 0,
          "type": "integer"
        },
        "maxTurnsPerAgent": {
          "description": "max_turns_per_agent caps the number of agent turns (Observe→Think→Act iterations) for any single agent node in the mission. 0 means unlimited.",
          "format": "int32",
          "minimum": 0,
          "type": "integer"
        },
        "requireEvidence": {
          "description": "require_evidence indicates whether all findings must include proof-of-concept evidence before being recorded.",
          "type": "boolean"
        },
        "severityThreshold": {
          "description": "severity_threshold is the minimum severity level required to record a finding. Common values: \"low\", \"medium\", \"high\", \"critical\". Empty string means accept all severities.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "MissionDependencies": {
      "additionalProperties": false,
      "description": "MissionDependencies specifies required components for a mission",
      "properties": {
        "agents": {
          "description": "Agents lists required agent components by name or URL",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "plugins": {
          "description": "Plugins lists required plugin components by name or URL",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "tools": {
          "description": "Tools lists required tool components by name or URL",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "MissionEdge": {
      "additionalProperties": false,
      "description": "MissionEdge represents a directed edge in the mission DAG.  The graph is acyclic. A loop is never an edge. Work that repeats until it passes an acceptance step loops INSIDE one node: a job node runs its verify loop against the same job (gibson#1706), and a node's RetryPolicy retries the node itself.",
      "properties": {
        "condition": {
          "description": "Condition is an optional condition that must be satisfied for the edge to be traversed",
          "type": "string"
        },
        "from": {
          "description": "From is the source node ID",
          "type": "string"
        },
        "metadata": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Metadata contains additional metadata for the edge",
          "type": "object"
        },
        "to": {
          "description": "To is the destination node ID",
          "type": "string"
        }
      },
      "type": "object"
    },
    "MissionNode": {
      "additionalProperties": false,
      "description": "MissionNode represents a single node in a mission DAG",
      "oneOf": [
        {
          "properties": {
            "agentConfig": {
              "$ref": "#/$defs/AgentNodeConfig",
              "description": "AgentConfig for agent nodes"
            }
          },
          "required": [
            "agentConfig"
          ]
        },
        {
          "properties": {
            "toolConfig": {
              "$ref": "#/$defs/ToolNodeConfig",
              "description": "ToolConfig for tool nodes"
            }
          },
          "required": [
            "toolConfig"
          ]
        },
        {
          "properties": {
            "pluginConfig": {
              "$ref": "#/$defs/PluginNodeConfig",
              "description": "PluginConfig for plugin nodes"
            }
          },
          "required": [
            "pluginConfig"
          ]
        },
        {
          "properties": {
            "conditionConfig": {
              "$ref": "#/$defs/ConditionNodeConfig",
              "description": "ConditionConfig for condition nodes"
            }
          },
          "required": [
            "conditionConfig"
          ]
        },
        {
          "properties": {
            "parallelConfig": {
              "$ref": "#/$defs/ParallelNodeConfig",
              "description": "ParallelConfig for parallel nodes"
            }
          },
          "required": [
            "parallelConfig"
          ]
        },
        {
          "properties": {
            "joinConfig": {
              "$ref": "#/$defs/JoinNodeConfig",
              "description": "JoinConfig for join nodes (mission-verb-noun-registry). Field number 15 because 10-14 are sibling MissionNode fields (dependencies, timeout, retry_policy, data_policy, metadata)."
            }
          },
          "required": [
            "joinConfig"
          ]
        },
        {
          "properties": {
            "jobConfig": {
              "$ref": "#/$defs/JobNodeConfig",
              "description": "JobConfig for job nodes (gibson#1706). Field number 17 because 16 is the sibling reuse_policy field."
            }
          },
          "required": [
            "jobConfig"
          ]
        }
      ],
      "properties": {
        "dataPolicy": {
          "$ref": "#/$defs/DataPolicy",
          "description": "DataPolicy defines data handling policy for this node.  Deprecated: data reuse + scoping are no longer node-declared. Under the ECS brain (gibson#851, ADR-0008) reuse is implicit in the event-sourced World and scoping flows from scope-relative identity (ADR-0002) + ambient projection. The field is retained wire-compatibly for old definitions but is ignored by the engine."
        },
        "dependencies": {
          "description": "Dependencies lists node IDs that must complete before this node executes",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "description": {
          "description": "Description provides additional context about this node",
          "type": "string"
        },
        "id": {
          "description": "ID is the unique identifier for this node within the mission",
          "type": "string"
        },
        "metadata": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Metadata contains additional custom metadata for this node",
          "type": "object"
        },
        "name": {
          "description": "Name is a human-readable name for the node",
          "type": "string"
        },
        "retryPolicy": {
          "$ref": "#/$defs/RetryPolicy",
          "description": "RetryPolicy defines retry behavior for this node"
        },
        "reusePolicy": {
          "$ref": "#/$defs/ReusePolicy",
          "description": "ReusePolicy declares how this node's I/O is scoped + reused across mission runs.  Deprecated: superseded by the ECS brain (gibson#851, ADR-0008). Reuse is implicit in the World; scoping is via scope-relative identity (ADR-0002) + ambient projection. Retained wire-compatibly but ignored by the engine."
        },
        "timeout": {
          "description": "Timeout is the maximum execution time for this node",
          "type": "string"
        },
        "type": {
          "description": "Type is the node type",
          "enum": [
            "NODE_TYPE_AGENT",
            "NODE_TYPE_TOOL",
            "NODE_TYPE_PLUGIN",
            "NODE_TYPE_CONDITION",
            "NODE_TYPE_PARALLEL",
            "NODE_TYPE_JOIN",
            "NODE_TYPE_JOB"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "ParallelNodeConfig": {
      "additionalProperties": false,
      "description": "ParallelNodeConfig contains configuration for parallel nodes. PARALLEL fans out to its sub-nodes concurrently, capped by max_concurrency. Sibling failures are isolated (one failing sub-node does not cancel its siblings). Spec: mission-verb-noun-registry Requirement 6.",
      "properties": {
        "maxConcurrency": {
          "description": "MaxConcurrency limits the number of concurrent executions (0 = unlimited)",
          "format": "int32",
          "type": "integer"
        },
        "subNodes": {
          "description": "SubNodes contains the nodes to execute in parallel",
          "items": {
            "$ref": "#/$defs/MissionNode"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "PluginNodeConfig": {
      "additionalProperties": false,
      "description": "PluginNodeConfig contains configuration for plugin nodes. PLUGIN = multi-method provider keyed by `plugin_name + method`. Distinct from TOOL: a plugin advertises several callable methods behind one component identity. The executor selects the named method and dispatches `params` as the call payload.",
      "properties": {
        "maxTokensPerCall": {
          "description": "max_tokens_per_call is the per-node override of MissionConstraints.max_tokens_per_call for this plugin node only. Follows the same semantics as AgentNodeConfig.max_tokens_per_call: present and non-zero caps the call; present and 0 disables the cap for this node; absent means fall through to the mission-level constraint. Spec: mission-schema-canonicalization Requirement 5; gibson#133.",
          "format": "int32",
          "minimum": 0,
          "type": "integer"
        },
        "method": {
          "description": "Method is the plugin method to call",
          "type": "string"
        },
        "params": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Params contains the method parameters",
          "type": "object"
        },
        "pluginName": {
          "description": "PluginName is the name of the plugin to query",
          "type": "string"
        }
      },
      "type": "object"
    },
    "RepositoryConfig": {
      "additionalProperties": false,
      "description": "RepositoryConfig defines a single repository to clone. Maps directly to the SDK's workspace.RepositoryConfig type.",
      "properties": {
        "branch": {
          "description": "Branch is the Git branch to checkout after cloning. Defaults to the repository's default branch when empty.",
          "type": "string"
        },
        "credentialName": {
          "description": "CredentialName references a credential in the credential store. Optional — public repos don't need credentials.",
          "type": "string"
        },
        "dependsOn": {
          "description": "DependsOn lists repository names that must clone first. Enables topological ordering for multi-repo missions.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "name": {
          "description": "Name is the unique identifier for this repository within the mission. Required.",
          "type": "string"
        },
        "shallow": {
          "description": "Shallow enables `git clone --depth 1`.",
          "type": "boolean"
        },
        "url": {
          "description": "URL is the Git repository URL (HTTPS or SSH). Required.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "RepositorySpec": {
      "additionalProperties": false,
      "description": "RepositorySpec names one repository a job works in and what leaves the sandbox at wrap-up.",
      "properties": {
        "baseBranch": {
          "description": "base_branch is the branch the job branch starts from and the merge request targets. Empty means the project default branch.",
          "type": "string"
        },
        "connectorRef": {
          "description": "connector_ref names the connector that holds the forge credential, in the slash form \"connector/\u003cname\u003e\". Required.",
          "minLength": 1,
          "type": "string"
        },
        "deliverable": {
          "description": "deliverable is what the member does with the work at wrap-up. Required.",
          "enum": [
            "DELIVERABLE_KIND_PUSH_BRANCH",
            "DELIVERABLE_KIND_MERGE_REQUEST",
            "DELIVERABLE_KIND_NONE"
          ],
          "type": "string"
        },
        "name": {
          "description": "name is the directory name of the worktree the member sees. Unique inside the job. Required.",
          "minLength": 1,
          "type": "string"
        },
        "project": {
          "description": "project is the project path on the forge, for example \"group/repo\". Required.",
          "minLength": 1,
          "type": "string"
        }
      },
      "type": "object"
    },
    "RetryPolicy": {
      "additionalProperties": false,
      "properties": {
        "backoffStrategy": {
          "description": "BackoffStrategy determines how delays are calculated between retries",
          "enum": [
            "BACKOFF_STRATEGY_CONSTANT",
            "BACKOFF_STRATEGY_LINEAR",
            "BACKOFF_STRATEGY_EXPONENTIAL"
          ],
          "type": "string"
        },
        "initialDelay": {
          "description": "InitialDelay is the delay before the first retry attempt",
          "type": "string"
        },
        "maxDelay": {
          "description": "MaxDelay is the maximum delay between retry attempts (used for exponential backoff)",
          "type": "string"
        },
        "maxRetries": {
          "description": "MaxRetries is the maximum number of retry attempts",
          "format": "int32",
          "type": "integer"
        },
        "multiplier": {
          "description": "Multiplier is the factor by which the delay increases (used for exponential backoff)",
          "format": "double",
          "type": "number"
        }
      },
      "type": "object"
    },
    "ReusePolicy": {
      "additionalProperties": false,
      "description": "ReusePolicy declares how a node's I/O is scoped + reused across mission runs.  Deprecated: superseded by the ECS brain (gibson#851, ADR-0008). Reuse is implicit in the event-sourced World and scoping flows from scope-relative identity (ADR-0002) + ambient projection, so node-declared reuse/scoping no longer has meaning. Retained wire-compatibly; ignored by the engine.",
      "properties": {
        "inputScope": {
          "description": "InputScope: \"mission_run\" | \"mission\" | \"global\". Default: \"mission\".",
          "type": "string"
        },
        "outputScope": {
          "description": "OutputScope: \"mission_run\" | \"mission\" | \"global\". Default: \"mission\".",
          "type": "string"
        },
        "reuse": {
          "description": "Reuse: \"skip\" | \"rerun\" | \"merge\". Controls behavior when an existing output is found in scope. Default: \"rerun\".",
          "type": "string"
        }
      },
      "type": "object"
    },
    "Task": {
      "additionalProperties": false,
      "description": "Task represents a goal-oriented task with context and constraints.  context is free-form: the daemon does not read it. Anything the daemon must enforce lives in a declared field. Resources a dispatched task needs (repositories, credential names, World inputs, acceptance) live in job, because the daemon reads them before it mints the task grant.",
      "properties": {
        "constraints": {
          "$ref": "#/$defs/TaskConstraints"
        },
        "context": {
          "additionalProperties": {
            "$ref": "#/$defs/TypedValue"
          },
          "description": "",
          "type": "object"
        },
        "goal": {
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "job": {
          "$ref": "#/$defs/JobSpec",
          "description": "job names the typed resources this task needs and how it is judged (gibson#1706). It is set when the task opens or continues a job on a bank member. goal stays the goal; the spec's own goal is ignored on a dispatched task. Absent on a task that needs no repository, no credential and no acceptance step."
        },
        "metadata": {
          "additionalProperties": {
            "$ref": "#/$defs/TypedValue"
          },
          "description": "",
          "type": "object"
        }
      },
      "type": "object"
    },
    "TaskConstraints": {
      "additionalProperties": false,
      "description": "TaskConstraints represents execution constraints for a task.",
      "properties": {
        "allowedTools": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "blockedTools": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxTokens": {
          "format": "int32",
          "type": "integer"
        },
        "maxTurns": {
          "format": "int32",
          "type": "integer"
        }
      },
      "type": "object"
    },
    "ToolNodeConfig": {
      "additionalProperties": false,
      "description": "ToolNodeConfig contains configuration for tool nodes. TOOL = single-purpose named function with a typed input map. The executor invokes the named tool through the tool worker queue and returns the tool's output as the node result.",
      "properties": {
        "input": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Input contains the tool input parameters",
          "type": "object"
        },
        "maxTokensPerCall": {
          "description": "max_tokens_per_call is the per-node override of MissionConstraints.max_tokens_per_call for this tool node only. Follows the same semantics as AgentNodeConfig.max_tokens_per_call: present and non-zero caps the call; present and 0 disables the cap for this node; absent means fall through to the mission-level constraint. Spec: mission-schema-canonicalization Requirement 5; gibson#133.",
          "format": "int32",
          "minimum": 0,
          "type": "integer"
        },
        "toolName": {
          "description": "ToolName is the name of the tool to execute",
          "type": "string"
        }
      },
      "type": "object"
    },
    "TypedArray": {
      "additionalProperties": false,
      "description": "TypedArray represents an array of TypedValues",
      "properties": {
        "items": {
          "items": {
            "$ref": "#/$defs/TypedValue"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "TypedMap": {
      "additionalProperties": false,
      "description": "TypedMap represents a map of string keys to TypedValues",
      "properties": {
        "entries": {
          "additionalProperties": {
            "$ref": "#/$defs/TypedValue"
          },
          "description": "",
          "type": "object"
        }
      },
      "type": "object"
    },
    "TypedValue": {
      "additionalProperties": false,
      "description": "TypedValue represents a dynamically typed value",
      "oneOf": [
        {
          "properties": {
            "nullValue": {
              "description": "NullValue represents a null value in TypedValue",
              "enum": [],
              "type": "string"
            }
          },
          "required": [
            "nullValue"
          ]
        },
        {
          "properties": {
            "stringValue": {
              "type": "string"
            }
          },
          "required": [
            "stringValue"
          ]
        },
        {
          "properties": {
            "intValue": {
              "format": "int64",
              "type": "integer"
            }
          },
          "required": [
            "intValue"
          ]
        },
        {
          "properties": {
            "doubleValue": {
              "format": "double",
              "type": "number"
            }
          },
          "required": [
            "doubleValue"
          ]
        },
        {
          "properties": {
            "boolValue": {
              "type": "boolean"
            }
          },
          "required": [
            "boolValue"
          ]
        },
        {
          "properties": {
            "bytesValue": {
              "contentEncoding": "base64",
              "type": "string"
            }
          },
          "required": [
            "bytesValue"
          ]
        },
        {
          "properties": {
            "arrayValue": {
              "$ref": "#/$defs/TypedArray"
            }
          },
          "required": [
            "arrayValue"
          ]
        },
        {
          "properties": {
            "mapValue": {
              "$ref": "#/$defs/TypedMap"
            }
          },
          "required": [
            "mapValue"
          ]
        }
      ],
      "type": "object"
    },
    "WorkspaceConfig": {
      "additionalProperties": false,
      "description": "WorkspaceConfig configures repository cloning + workspace management for missions whose agents need to interact with code. The daemon's workspace manager honors this at mission start (initializeWorkspaces).  Spec: mission-schema-canonicalization (mirror migration — lifts mission.WorkspaceConfig from the daemon's hand-written mirror into the canonical proto schema).",
      "properties": {
        "repositories": {
          "description": "Repositories to clone for this mission. Each entry becomes a workspace addressable from agents via harness.Workspace(name).",
          "items": {
            "$ref": "#/$defs/RepositoryConfig"
          },
          "type": "array"
        },
        "settings": {
          "$ref": "#/$defs/WorkspaceSettings",
          "description": "Settings carries workspace-wide knobs (cleanup, LSP, isolation)."
        }
      },
      "type": "object"
    },
    "WorkspaceSettings": {
      "additionalProperties": false,
      "description": "WorkspaceSettings carries workspace-wide options.",
      "properties": {
        "baseDirectory": {
          "description": "BaseDirectory is the workspace clone root. When empty the daemon uses a temp directory.",
          "type": "string"
        },
        "cleanupOnComplete": {
          "description": "CleanupOnComplete deletes workspace directories after the mission ends. Defaults to true at the daemon when unset (cleanup is the safe default).",
          "type": "boolean"
        },
        "lspEnabled": {
          "description": "LSPEnabled starts language servers for code validation.",
          "type": "boolean"
        },
        "lspTimeout": {
          "description": "LSPTimeout caps LSP validation duration. Encoded as a protobuf Duration to preserve sub-second precision.",
          "type": "string"
        },
        "useWorktrees": {
          "description": "UseWorktrees enables Git worktrees for per-agent isolation — concurrent modifications without conflicts.",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  "$id": "https://gibson.zeroroot.ai/schemas/mission-definition.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "MissionDefinition represents a mission template/definition. This is the shareable mission specification that can be created via the CreateMissionDefinition API and referenced by mission runs.",
  "properties": {
    "constraints": {
      "$ref": "#/$defs/MissionConstraints",
      "description": "Constraints declares mission-level operational limits. When present, these are the authoritative constraints for the mission DAG — they make the mission self-describing so authors do not have to supply limits out-of-band at dispatch time. The daemon merges these with any dispatch-time overrides (dispatch wins on conflict). Optional: absent means no constraints are baked into the definition.  Spec: sdk#47 (MissionConstraints proto promotion)."
    },
    "createdAt": {
      "description": "CreatedAt is the timestamp when the mission definition was created",
      "format": "date-time",
      "type": "string"
    },
    "deciderSlot": {
      "$ref": "#/$defs/LLMSlotConfig",
      "description": "DeciderSlot names the mission-level LLM the brain's Decider runs on — the orchestration decision-maker, distinct from per-node agent slots (gibson#850). Only the provider+model are used (the slot name is implicitly \"decider\"). Absent means the brain uses the tenant's dashboard-default provider/model, so missions need not set it."
    },
    "dependencies": {
      "$ref": "#/$defs/MissionDependencies",
      "description": "Dependencies specifies required agents and tools for this mission"
    },
    "description": {
      "description": "Description provides additional context about what this mission does",
      "type": "string"
    },
    "edges": {
      "description": "Edges contains all the directed edges connecting nodes in the mission",
      "items": {
        "$ref": "#/$defs/MissionEdge"
      },
      "type": "array"
    },
    "entryPoints": {
      "description": "EntryPoints contains the IDs of nodes that can serve as entry points to the mission These are nodes with no incoming edges",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "exitPoints": {
      "description": "ExitPoints contains the IDs of nodes that can serve as exit points from the mission These are nodes with no outgoing edges",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "id": {
      "description": "ID is the unique identifier for this mission definition",
      "type": "string"
    },
    "installedAt": {
      "description": "InstalledAt is the timestamp when this mission was installed",
      "format": "date-time",
      "type": "string"
    },
    "metadata": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "Metadata contains additional custom metadata for the mission",
      "type": "object"
    },
    "name": {
      "description": "Name is a human-readable name for the mission",
      "type": "string"
    },
    "nodes": {
      "additionalProperties": {
        "$ref": "#/$defs/MissionNode"
      },
      "description": "Nodes contains all the nodes in the mission, indexed by node ID",
      "type": "object"
    },
    "source": {
      "description": "Source is the git URL this mission was installed from (if applicable)",
      "type": "string"
    },
    "targetRef": {
      "description": "TargetRef is a reference to the target (name or ID) This needs to be resolved to a TargetID when creating a mission instance",
      "type": "string"
    },
    "version": {
      "description": "Version is the semantic version of the mission definition",
      "type": "string"
    },
    "workspace": {
      "$ref": "#/$defs/WorkspaceConfig",
      "description": "Workspace configures repository cloning + workspace management for agents that need code access. Optional; missions without code interaction omit this field.  Spec: mission-schema-canonicalization (mirror migration)."
    }
  },
  "title": "MissionDefinition",
  "type": "object"
}
