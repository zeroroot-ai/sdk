name: release-please

on:
  push:
    branches: [main]

# Permissions on the default GITHUB_TOKEN. These cover release-please's PR/commit
# work, but the *tag push* is done with the zeroday-sdk-fanout App token below
# so that fan-out.yml (on.push.tags) actually triggers — tags pushed by
# GITHUB_TOKEN do not fire downstream workflows.
permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    name: release-please
    runs-on: ubuntu-latest
    steps:
      - name: Mint App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3
        with:
          app-id: ${{ secrets.SDK_FANOUT_APP_ID }}
          private-key: ${{ secrets.SDK_FANOUT_APP_PRIVATE_KEY }}
          owner: zeroroot-ai
          repositories: sdk

      - uses: googleapis/release-please-action@45996ed1f6d02564a971a2fa1b5860e934307cf7 # v5
        with:
          token: ${{ steps.app-token.outputs.token }}
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json
